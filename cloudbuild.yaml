steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    id: 'Build Docker image'
    args: ['build', '-t', 'asia-southeast1-docker.pkg.dev/classconnect-455912/classconnectrepo/classconnectimg:$COMMIT_SHA', '.']
  
  # Push the container image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    id: 'Push Docker image'
    args: ['push', 'asia-southeast1-docker.pkg.dev/classconnect-455912/classconnectrepo/classconnectimg:$COMMIT_SHA']
  
  # Deploy to Cloud Run with secrets
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'Deploy to Cloud Run'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        gcloud run deploy classconnect \
          --image=us-west2-docker.pkg.dev/classconnect-455912/classconnectrepo/classconnectimg:$COMMIT_SHA \
          --region=us-west2 \
          --platform=managed \
          --update-secrets=AZURE_SPEECH_KEY=AZURE_SPEECH_KEY:latest \
          --update-secrets=AZURE_SPEECH_REGION=AZURE_SPEECH_REGION:latest \
          --update-secrets=OPENAI_API_KEY=OPENAI_API_KEY:latest \
          --update-secrets=SUPABASE_URL=SUPABASE_URL:latest \
          --update-secrets=SUPABASE_KEY=SUPABASE_KEY:latest

images:
  - 'us-west2-docker.pkg.dev/classconnect-455912/classconnectrepo/classconnectimg:$COMMIT_SHA'

options:
  logging: CLOUD_LOGGING_ONLY